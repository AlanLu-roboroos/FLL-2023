{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Upload File",
      "type": "shell",
      "command": "${workspaceFolder}/upload.sh apollo ${relativeFile}",
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "Run",
      "type": "shell",
      "command": "ssh robot@apollo.local 'brickrun -r -- pybricks-micropython -i ~/FLL-2023/main.py'",
    }
  ]
}